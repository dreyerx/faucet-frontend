/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
        base_api: "https://faucet-api.dreyerx.com",
        explorer_url: "https://testnet-scan.dreyerx.com"
    }
};

export default nextConfig;
